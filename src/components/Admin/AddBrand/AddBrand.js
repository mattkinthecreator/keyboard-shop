import React from 'react'
import { db } from '../../../fire'

const AddBrand = () => {
  return (
    <div>
      <h2>Brands</h2>
      <button>Add brand</button>
    </div>
  )
}

export default AddBrand
